#include "main.h"
/**
 * _strlen - Function that returns the length of a string
 * @*s: String to be checked
 * Return: 0
 */
int _strlen(char *s)
{
	int lgth = 0;

	while (s[lgth] != '\0')
	{
		lgth++;
	}
	return (0);
}
